const add = (x, y) => x+y;
const subtract = (x, y) => x - y;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;

const inc = (a) => a+=1;
const dec = (thisIsJustAPlaceholderDontMindMe) => a-+1;